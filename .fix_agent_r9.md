You are a Fable 5 fix agent for the Omarchy plugin competition. Work ONLY inside the plugin directory given below. Grok wrote the initial implementation; GPT-5.6 Sol reviewed it and it FAILED. Your job: apply the fixes to pass the gate.

PLUGIN DIR: /Users/chris/Developer/OmarchyPlugins/rewind
SPEC (authoritative; its "Tribunal review" applied-changes section supersedes earlier contradictions): /Users/chris/Developer/OmarchyPlugins/docs/10-rewind.md
PLATFORM REFERENCE (authoritative for manifest/IPC/settings): /Users/chris/Developer/OmarchyPlugins/docs/quattro-shell-reference.md

CONSTRAINTS:
- macOS build host: you CANNOT run Hyprland/Quickshell/qmllint/cargo-on-target. Write for correctness by inspection; keep QML conservative (Quickshell.Io Process/Socket/FileView, Quickshell.Hyprland, standard QtQuick). Do NOT invent Quickshell APIs; prefer documented IPC (omarchy-shell shell call/summon/hide/toggle) over undocumented host shortcuts (serviceFor etc.).
- Linux prebuilt binaries cannot be produced here — use a CI release workflow + honest source-build docs; never commit fake binaries.
- Do NOT delete review/log dotfiles; keep them gitignored.
- Every fix must be real and complete (no TODO/stubs in core paths). Add/repair regression tests for each fixed blocker where a test harness exists (node for JS engines, cargo tests for Rust, .sh for shell).

TASK: Read the GPT review below (the latest .gpt_review_r*.md), fix EVERY blocker and address the warnings. Re-run whatever tests run off-device and confirm green. Update ASSUMPTIONS.md. Then `git commit` with a message like "fix: address review round N (fable)".

Your final message: a terse list of what you changed per blocker, and any blocker you believe is a false positive (with reasoning).

REVIEW TO ADDRESS:
GATE: FAIL
BLOCKERS:
1. `Overlay.qml:376-386; js/Plan.js:46-101; src/rewindd/src/lib.rs:437-482` — Per-item Reopen is nonfunctional: stored windows contain no `exec`/`cmd`, so the QML plan sends an empty launch command, while stale historical addresses cannot identify live windows. Build the one-window plan in the helper using desktop-file mapping and current clients, then return it for review before execution.
2. `compat/rewindd.sh:81-104,484-499` — The missing-binary fallback still persists consent by writing directly to `state.json`, suppresses write errors, and can leave truncated state. Implement private temp-file write, flush/fsync, atomic rename, error propagation, and consent rollback on failure.
3. `src/rewindd/src/lib.rs:569-589,700-826` — `still_armed()` ignores its generation argument. A capture or OCR job started before disarm can survive a rapid disarm/re-arm and commit stale material from across the disarmed boundary. Require `arm_gen == gen` alongside the armed checks and add a disarm-then-rearm mid-operation regression.
4. `scripts/network-audit.sh:50-80; .github/workflows/linux-helper.yml:52-53` — The claimed full-capture no-network proof can pass without capturing any frame; it checks only readiness, replies, and shutdown. Run the audit with a real or deterministic test capture backend and require `frame-written` or a positive frame count before accepting the strace result.
5. `src/rewindd/src/store.rs:459-546` — Byte-cap pruning occurs after the capture transaction, and its failure is ignored. This violates the specified insert-transaction cap and can report successful captures while storage remains over limit. Perform oldest-first pruning inside the same transaction and propagate failures.
WARNINGS:
1. `src/rewindd/src/config.rs:77-108` — Temp-file fsync and rename are present, but the parent directory is not fsynced after rename.
2. `scripts/seed-demo.sh:16-18` — User-controlled phrase/hash values are interpolated into shell commands without robust escaping.
3. `compat/rewindd.sh:43` — Fallback exclusion defaults remain narrower than the manifest/Rust defaults.
SUMMARY: The five round-8 fixes are visibly attempted, but the per-item action and fallback atomic persistence remain incomplete in real execution paths. Fresh review also found stale-operation authorization, a non-proving network audit, and non-atomic byte-cap enforcement that can fail on a judge’s machine.
